apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: ginfos.nameof.github.com
spec:
  group: nameof.github.com
  names:
    kind: GithubInfo
    listKind: GithubInfoList
    plural: ginfos
    singular: ginfo
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                username:
                  type: string
                link:
                  type: string
                repoCount:
                  format: int32
                  type: integer
              required:
                - username
                - link
                - repoCount
      additionalPrinterColumns:
        - name: CR-Name
          type: string
          jsonPath: .spec.username
